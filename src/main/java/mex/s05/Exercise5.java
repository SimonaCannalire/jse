package mex.s05;

public class Exercise5 {
	/**
	 * Check for anagram
	 * 
	 * @param s
	 * @param t
	 * @return true if s is an anagram of t
	 */
	public static boolean isAnagram(String s, String t) {
		if (s == null && t == null || s.length() == 0 && s.length() == 0) {
			return false;
		
		}
	}
}
